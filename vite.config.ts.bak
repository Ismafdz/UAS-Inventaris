import { defineConfig } from 'vite'
import { adonisVitePlugin } from '@adonisjs/vite'

export default defineConfig({
  plugins: [
    adonisVitePlugin({
      entrypoints: ['resources/css/app.css', 'resources/js/app.js'],
      reload: ['resources/views/**/*.edge'],
    }),
  ],
})
