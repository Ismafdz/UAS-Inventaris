@layout('components/layouts/main')
@set('title', 'Edit Barang')

@section('content')
<h3 class="mb-4">Edit Barang</h3>
<div class="card">
  <div class="card-body">
    <form action="{{ route('barang.update', { id: barang.id }) }}?_method=PUT" method="POST">
      @include('pages/barang/partials/form')
    </form>
  </div>
</div>
@endsection
